# HACKMAN 프론트엔드 디자인 기준 (확정, 2026-09-06)

> **이 문서가 화면 디자인의 단일 기준이다.** 새 화면을 추가하거나 기존 화면을 고칠 때는
> 여기 적힌 토큰·규격·패턴을 그대로 따른다. 값을 바꿔야 하면 화면이 아니라 이 문서를 먼저
> 고친다.
>
> 확정 디자인 캔버스(목록·상세·심사·로그인·시상식, 라이트/다크 10화면):
> https://claude.ai/code/artifact/6c81e163-60d5-488d-a916-69f2c43bfa9f

## 컨셉

마케팅 랜딩이 아니라 "라이브 운영 데스크". 대회 생성 → 팀 구성 → 심사위원 채점 → 실시간
스코어보드 → 시상으로 이어지는, 시간에 쫓기며 숫자를 계속 확인하는 도구. 스코어보드가
페이지의 무게중심이며, 리스트 맨 아래 부록이 되면 안 된다.

문서·코드를 다루는 도구답게 GitHub 계열의 밀도 있는 정보 배치를 참고한다 — 상단 브레드크럼
바, 밑줄 탭, 행 hover, 파일트리 + 라인번호 코드 뷰. 단, 색과 서체는 아래 토큰을 벗어나지
않는다.

## 색상

라이트가 기준이고, 다크는 같은 색상(hue)에서 명도만 올린다. 실제 값은
`frontend/src/style.css`의 `:root` / `[data-theme='dark']`에 있다.

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--ink` | `#14181C` | `#EDEEEA` | 본문·제목 |
| `--paper` | `#F3F4F1` | `#14161A` | 배경 |
| `--paper-raised` | `#FAFAF8` | `#1B1E22` | 행 hover, 보조 면 |
| `--surface` | `#FFFFFF` | `#202327` | 입력창·패널 바닥 |
| `--line` | `#D8DAD4` | `#33373C` | 헤어라인 |
| `--live` | `#2F6F4F` | `#3FA372` | 모집중·진행중, LIVE 신호 |
| `--judging` | `#C4622A` | `#E08148` | 심사중, 마감 임박 경고 |
| `--signal` | `#1F3A5F` | `#6C93C7` | 점수·순위 등 데이터 강조 |
| `--muted` | `#6B6F6A` | `#9AA0A6` | 보조 텍스트 |

- 채도는 "지금 라이브"일 때만 쓴다. 나머지는 그레이/잉크 톤.
- 크림+테라코타(`#D97757` 계열), 그라디언트 배경은 쓰지 않는다.
- 시상식 무대는 프로젝터 화면이므로 **테마와 무관하게 어두운 배경 고정**(`#14181c` 바닥 /
  `#f3f4f1` 글자). 이때 액센트는 다크 값(`#3FA372`·`#E08148`·`#6C93C7`)을 쓴다.

## 타이포그래피

두 서체, 역할이 명확히 구분된다.

- **Space Grotesk** — 제목·UI 텍스트
- **IBM Plex Mono** — 점수·시각·순위·날짜 등 실제 숫자 데이터 전용. 탭 정렬로 스코어보드가
  전광판처럼 읽힌다. 상태 라벨 같은 장식 목적으로는 쓰지 않는다.

### 타입 스케일 (2026-09-06 확대 확정)

기준 크기를 한 단계 키웠다. `:root { font-size: 19px }`가 전체 스케일 레버이고, 개별 값은
전부 `rem`으로 적는다 — 화면마다 px를 직접 박지 않는다.

| 역할 | rem | 실제 크기 |
|---|---|---|
| 화면 제목 | `1.6rem` | 약 30px |
| 섹션 제목 | `1.05rem` | 약 20px |
| 본문·리스트 항목 | `0.9rem` | 약 17px |
| 보조 텍스트·메타 | `0.82rem` | 약 16px |
| 모노 라벨·캡션 | `0.75rem` | 약 14px |
| 스코어보드 숫자 | `1.1rem` 이상 | 21px 이상 |

프로젝터에 띄우는 시상식 화면은 `clamp()`로 더 크게 간다(상 이름 최대 4rem, 팀 이름 최대 3rem).

## 레이아웃

- **상단 바** — 높이 68px, 아래에 헤어라인 1px. 왼쪽에 로고 마크(28px 잉크 사각형 + 모노 `H`)
  와 워드마크, 그다음 `/` 구분자로 브레드크럼(대회명 → 화면명). 오른쪽에 테마 토글과 계정.
- **본문** — 좌측 정렬. 중앙 정렬 카드 나열은 쓰지 않는다. 컨테이너 최대 폭 860–1080px.
- **리스트** — 카드+그림자 대신 헤어라인으로 구분한 행. 행 hover 시 `--paper-raised`.
- **상태 표시** — 둥근 배지 대신 **왼쪽 컬러 레일(3–4px) + 점(8px)** + 텍스트 라벨.
  종료는 빗금 패턴으로 비활성임을 드러낸다.
- **탭** — 밑줄 탭(활성 항목만 잉크색 텍스트 + 2px 밑줄). 알약형 세그먼트는 쓰지 않는다.
- **스코어보드** — 상세 화면 최상단. 숫자는 우측 정렬 + 모노. 순위는 동점 공동 순위(1, 1, 3),
  점수 없는 팀은 순위 없이 맨 아래.
- **코드 열람** — 왼쪽 파일트리(모노) + 오른쪽 라인번호 코드 뷰. 문법 하이라이팅은 범위 밖.

## 컨트롤 규격

- 누를 수 있는 요소의 최소 높이는 **44px**(모바일 탭 타깃). 아이콘 버튼은 36px 정사각.
- 입력창 높이 44px, 좌우 패딩 12px, `border-radius` 2–6px. 그 이상 둥글리지 않는다.
- 1차 버튼은 잉크 배경 + 페이퍼 글자. 2차 버튼은 라인 테두리 + 투명 배경.
- 비활성 버튼은 `opacity: .5` + `cursor: not-allowed`로, 왜 잠겼는지 옆에 한 줄로 알린다.

## 아이콘 · 모션

- 아이콘은 **인라인 SVG만** 쓴다(스트로크 1.4px, 16/20/24px 그리드). 이모지·딩벳 문자는
  콘텐츠에도 UI에도 쓰지 않는다.
- 모션은 상태 변화를 알릴 때만 쓴다:
  - `LIVE` 표시의 느린 점멸(1.6s)
  - 시상식 **수상팀 공개** 시 컨페티 낙하 + 링 확산 + 수상 정보 상승
  - 그 외 화면 전환·hover에 애니메이션을 붙이지 않는다.
- 컨페티 색은 팔레트(`live`/`judging`/`signal`/`paper`)만 쓴다. 무지개색 금지.

## 화면별 기준

| 화면 | 핵심 |
|---|---|
| 대회 목록 | 헤어라인 행 + 상태 레일. 운영자에게만 `+ 새 대회 만들기`. 상태 필터는 밑줄 탭. |
| 대회 상세 | 스코어보드가 히어로. 상단에 상태 스테퍼(다음 단계만 활성) + 진행중일 때 카운트다운. |
| 심사하기 | 팀별 접이식 행. 펼치면 채점 폼 + 웹 데모/코드 탭. 심사중이 아니면 폼 잠금 + 사유 표시. |
| 로그인 | 좁은 단일 컬럼. 로그인/회원가입 밑줄 탭, 8자 미만·확인 불일치는 입력 즉시 안내. 로그인 없이 열람 가능함을 명시. |
| 시상식 | 어두운 무대 고정. 낮은 등수부터 → 대상. `상 이름 공개 → 수상팀 공개 → 다음 시상`. 해당 등수 팀이 없으면 "순위 정보 없음". |

## 하지 않는 것

1. 카드 + 그림자 반복, 모든 요소 동일한 12px 라운드
2. 그라디언트 배경, 유리 효과
3. 이모지 아이콘 (🎉 포함)
4. Inter·Roboto·Arial 등 기본값 서체
5. 상태를 알약 배지로 표시하기
6. 숫자가 아닌 곳에 모노스페이스 쓰기

## 구현 대상 파일

- `frontend/src/style.css` — 토큰·타입 스케일·컴포넌트 스타일의 단일 소스
- `frontend/src/App.tsx` — 상단 바, 대회 목록
- `frontend/src/ContestDetail.tsx` — 스코어보드 히어로, 상태 스테퍼, 심사·배정 패널
- `frontend/src/AuthPanel.tsx` — 로그인/회원가입 폼과 입력 검증 안내
- `frontend/src/AwardCeremony.tsx` — 시상 관리와 시상식 무대(컨페티 포함)
- `frontend/index.html` — Google Fonts (Space Grotesk, IBM Plex Mono)
