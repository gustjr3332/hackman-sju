asgiref==3.12.1
dj-database-url==3.1.2
Django==6.1
django-cors-headers==4.9.0
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
gunicorn==26.2.0
psycopg2-binary==2.9.12
python-dotenv==1.2.3
sqlparse==0.6.0
tzdata==2026.3
whitenoise==6.12.0

# LLM 제공사 SDK. 키가 설정된 제공사만 실제로 쓰이고, import 는 호출 시점까지 미룬다
# (contests/llm/base.py) - 안 쓰는 제공사 때문에 콜드 스타트가 늘지 않게 한다.
# openai 는 아직 키가 없어 지금은 쓰이지 않는다 - GPT 유료 키를 넣을 때를 위해 미리 둔다.
# anthropic 은 아직 키가 없어 넣지 않았다 - 쓸 때 추가한다.
google-genai==2.22.0
openai==3.8.0
